pipeline {
  /* 
  agent {
    label 'python'
  } 
  */

  agent any
  
  stages {
    stage('Build') {
      steps {
        // sh 'git checkout develop'
        echo 'Build stage'
      }
    }
    stage('Test') {
      steps {
        // sh 'git checkout develop'
        echo 'Test stage'
      }
    }
    stage('Deploy') {
      steps {
        // sh 'git checkout develop'
        echo 'Deploy stage'
      }
    }
  }
}
